$(document).ready(function() { // lorsque le DOM est chargé intégralement


  // switch carousel image every 2 seconds
  $('.carousel').carousel({
    interval: 5000
  })

  // display carousel when user click on button below the images

  // Failed


});
